    

"""
        Example of get_absolute_url method:

            def get_absolute_url(self):
            return u'/some_url/%d' % self.id 



    def get_success_url(self):
        return reverse("games:new-games", args=(self.id,))

    def get_absolute_url(self):
   
   
        return redirect('/games/', args=(self.id, ))

          def get_absolute_url(self):
            return redirect('')

"""

   
"""
    def get_queryset(self):
        # Get the travels for the user
        return Games.objects.filter(assigned_driver=self.request.user)
"""
"""
    def get_context_data(self, **kwargs):

        context = super(DriverTravelList, self).get_context_data(**kwargs)
        context.update({
            "current_time": timezone.now(),
        })
        return context
"""